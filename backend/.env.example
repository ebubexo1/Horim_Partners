# ===== SERVER =====
PORT=5000
NODE_ENV=production

# ===== MONGODB =====
# Get this from MongoDB Atlas -> Connect -> Drivers
# Example: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/horim-partners?retryWrites=true&w=majority
MONGO_URI=your_mongodb_atlas_connection_string_here

# ===== AUTH =====
JWT_SECRET=replace_with_a_long_random_secret_string
JWT_EXPIRES_IN=7d

# ===== PAYSTACK =====
# From Paystack Dashboard -> Settings -> API Keys & Webhooks
# NEVER put the secret key in frontend code. It only lives here, on the server.
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===== FRONTEND URL (for CORS + Paystack callback redirect) =====
# Your deployed Vercel URL, e.g. https://horim-partners.vercel.app
CLIENT_URL=http://localhost:5173

# ===== ONE-OFF SEED ROUTE (for hosts with no shell access, e.g. Render free tier) =====
# Set this to any long random string, then visit:
#   https://YOUR-RENDER-URL.onrender.com/api/dev/seed?token=THIS_VALUE
# once in your browser to seed the database. Remove this var afterwards to lock the route.
SEED_TOKEN=change_this_to_something_random_and_long

# ===== ADMIN SEED (used by `npm run seed` locally, or the /api/dev/seed route above) =====
SUPER_ADMIN_EMAIL=admin@horimpartners.org
SUPER_ADMIN_PASSWORD=ChangeThisPassword123!
SUPER_ADMIN_NAME=Horim Super Admin
